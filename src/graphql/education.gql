query EducationPage($locale: SiteLocale!) {
    education {
        text(locale: $locale)
        title(locale: $locale)
        timeline(locale: $locale) {
            title
            tags
            subtitle
            startDate
            fullDescription
            endDate
        }
        seo(locale: $locale) {
            title
            description
        }
    }
}
