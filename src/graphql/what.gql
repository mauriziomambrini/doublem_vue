query WhatPage($locale: SiteLocale!) {
    what {
        text(locale: $locale)
        title(locale: $locale)
        seo(locale: $locale) {
            title
            description
        }
    }
}
