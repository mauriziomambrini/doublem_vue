query AboutPage($locale: SiteLocale!) {
    about {
        text(locale: $locale)
        title(locale: $locale)
        seo(locale: $locale) {
            title
            description
        }
    }
}
